google-genai>=1.0.0
customtkinter>=5.2.2
requests>=2.31.0
readability-lxml>=0.8.1
lxml[html_clean]>=5.2.0
lxml_html_clean>=0.4.0
beautifulsoup4>=4.12.0
Pillow>=10.0.0
# chardet >= 5.x ships mypyc-compiled .pyd files that crash inside
# PyInstaller-frozen binaries with 0xC0000005. readability-lxml imports
# chardet directly, so we have to keep it -- pin to the last pure-Python
# release so the frozen exe doesn't crash on import.
chardet<5.0

